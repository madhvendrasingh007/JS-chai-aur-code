// Numbers:
let length = 16;

// Strings:
let color = "Yellow";

// Booleans
let x = true;

// Object:
const person = {firstName:"John", lastName:"Doe"};

// null => standalone value / it is not undefined but it is empty
let num = null;

// undefined => declared the variable but didn't assigned the value to it
let state;

// symbol => unique


console.log(person)
console.table([length, color, x, num, state])

console.log(typeof "madhvendra")    // string type
console.log(typeof length)  // number type
console.log(typeof person)  // object type
console.log(typeof null)    // object type
console.log(typeof undefined)   // undefined type